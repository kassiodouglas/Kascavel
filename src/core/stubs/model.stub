from orator import DatabaseManager, Model

#Conexão do model
from src.app.configs.database import CONN_DEFAULT
Model.set_connection_resolver(DatabaseManager(CONN_DEFAULT))

class %NAMECLASS%(Model):

    __table__ = '%NAMECLASS%'